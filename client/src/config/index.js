import { theme, useStylesTheme, styleModal, styleModalReport } from './theme.config';
import { changeTypeUser, returnTypeUser } from './changeTypeUser.config';
import environment from './environment.config';

export {
    theme,
    useStylesTheme, 
    changeTypeUser,
    returnTypeUser,
    styleModal,
    styleModalReport,
    environment
}
