import axios from 'axios'

export default {
    uploadImageProfile: (file, userId, imageFormat) => {
        //alert('En desarrollo')
        /* let formData = new FormData() 
        formData.append('image', file);
        formData.append('name', 'image.jpg');
        let config = {
            headers: {
              'Content-Type': 'multipart/form-data'
            }
        }
        return axios.post('/blob/uploadImageProfile', formData, config) */
    },
}